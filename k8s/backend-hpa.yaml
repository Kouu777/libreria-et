apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: libreria-backend-hpa
  namespace: libreria
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: libreria-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70